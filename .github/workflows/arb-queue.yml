name: ADR ARB queue (managed issue)

on:
  workflow_dispatch: {}

permissions:
  contents: read
  issues: write

jobs:
  queue:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - id: queue
        uses: mbeacom/adrkit/packages/ci/queue@896391cc385798f7f08c5694f70acaf0342789e9
        with:
          dir: docs/adr
      - name: Verify managed queue issue
        env:
          GH_TOKEN: ${{ github.token }}
          ISSUE_NUMBER: ${{ steps.queue.outputs.issue-number }}
        run: ./scripts/verify-managed-queue-issue.sh
